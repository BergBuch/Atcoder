#include<bits/stdc++.h>
using namespace std;

int main(){
    int ha,wa;
    std::cin >> ha >> wa;
    vector<string> A(ha);
    for(int w=0; w<wa; w++) std::cin >> A[w];

    int hb,wb;
    std::cin >> hb >> wb;
    vector<string> B(hb);
    for(int w=0; w<wb; w++) std::cin >> B[w];

    int hx,wx;
    std::cin >> hx >> wx;
    vector<string> X(hx);
    for(int w=0; w<wx; w++) std::cin >> X[w];

    
}
